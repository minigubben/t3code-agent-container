#!/usr/bin/env bash
set -euo pipefail

HOST_CODEX_BINARY="${HOST_CODEX_BINARY:-codex}"

if ! command -v "${HOST_CODEX_BINARY}" >/dev/null 2>&1; then
  printf 'host codex binary not found on PATH: %s\n' "${HOST_CODEX_BINARY}" >&2
  exit 127
fi

exec "${HOST_CODEX_BINARY}" "$@"
